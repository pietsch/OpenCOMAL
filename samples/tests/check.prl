       10 // check
       20 // External procedure used in checking programs
       30 //
       40 PROC check(truth#)
       50   IF NOT(truth#) THEN
       60     PRINT "Check failed"
       70     STOP
       80   ENDIF
       90 ENDPROC
